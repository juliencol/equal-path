const skill = (el) => {
  const greenButton = document.getElementById(el);
  if (greeButton) {
     greenButton.classList.toggle('grey-button');
     greenButton.classList.toggle('green-button');
  }
};

export { skill };
