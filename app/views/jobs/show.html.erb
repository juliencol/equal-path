<div class="banner-show-user" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= cl_image_path @job.job_photo %>);">
  <h1 class="text-center"><%= @job.title %></h1>

  <% if @job.bookmarked %>
    <%= link_to job_bookmark_job_path(@job), method: :post, remote: true do %>
      <i class="fas fa-star" id="star-user-show-two"></i>
    <% end %>
  <% else %>
    <%= link_to job_bookmark_job_path(@job), method: :post, remote: true do %>
      <i class="far fa-star" id="star-user-show-two"></i>
    <% end %>
  <% end %>

</div>

<div class="user-container">
  <ul class="list-inline tabs-underlined">
    <li>
      <a href="#" class="tab-underlined active" data-target="Fiche">Ficher métier</a>
    </li>
    <li>
      <a href="#" class="tab-underlined" data-target="Formations">Formations</a>
    </li>
    <li>
      <a href="#" class="tab-underlined" data-target="Mentors">Mentors</a>
    </li>
  </ul>

  <div class="tab-content" id="Fiche">
    <div id="top-block" class="container d-flex p-3">
      <div id="left-block" class="container d-flex flex-column mr-3">
        <div class="d-flex">
          <h2 class = "mb-5"> 📝 Description du métier</h2>
        </div>
        <p class=" text-justify"><%= @job.long_description %></p>
      </div>

      <div id="right-block" class="container d-flex flex-column ml-3">
        <div class="mb-3 container d-flex">
          <div>
            <div class="d-flex align-items-center">
            <h2> Pour ce travail, il faut : </h2>
          </div>
          <div class="container d-flex mt-5 justify-content-between">
                <% @job.skills.each do |skill| %>
                  <div class="d-flex justify-content-around">
                    <% if @user.skills.include? skill %>
                     <button class="btn btn-outline-success m-2" data-toggle="modal" data-target="#skill-modal-<%= skill.id %>">
                         <%= skill.name %>
                      </button>
                    <% end  %>
                  </div>
                  <!-- modal -->
                  <div class="modal fade" id="skill-modal-<%= skill.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                      <div class="modal-content">
                        <div class="modal-body text-center">
                           <h1 class="m-3"><%= skill.name %></h1>
                            <img id="skill-logo" class="m-3" src=<%= skill.logo %>>
                              <p class="m-3"><%= skill.description %></p>
                        </div>
                      </div>
                    </div>
                  </div>
                  <!-- fin modal -->
                <% end %>

              <% @job.skills.each do |skill| %>
                <div class="d-flex justify-content-around">
                  <% if @user.skills.exclude? skill %>
                    <button type="button" class="btn btn-outline-warning m-2" data-toggle="modal" data-target="#skill-modal-<%= skill.id %>">
                      <%= skill.name %>
                    </button>
                  <% end %>
                </div>
              <!-- modal -->
                  <div class="modal fade" id="skill-modal-<%= skill.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel" aria-hidden="true">
                    <div class="modal-dialog" role="document">
                      <div class="modal-content">
                        <div class="modal-body text-center">
                            <h1 class="m-3"><%= skill.name %></h1>
                            <img id="skill-logo" class="m-3" src=<%= skill.logo %>>
                              <p class="m-3"><%= skill.description %></p>
                        </div>
                      </div>
                    </div>
                  </div>
                  <!-- fin modal -->
                <% end %>
              </div>
          </div>
        </div>
      </div>
    </div>
     <div class="mt-5">
       <%= link_to "Revenir à l'index", jobs_path, class: "btn-prk text-decoration-none" %>
     </div>
  </div>


  <div id="Formations" class=" tab-content">
    <div class="container d-flex flex-column mt-5">
      <div class="d-flex align-items-center">
        <h2 class=""> 🎓 Formations recommandées</h2>
      </div>
      <div id="logo-container" class="container d-flex">
        <% @job.formations.each do |formation|%>
          <div class="container" data-toggle="modal" data-target="#school-modal-<%= formation.id %>">
            <img id="formation-logo" class= "rounded-logo" src=<%= formation.logo %>>
          </div>
          <div class="modal fade" id="school-modal-<%= formation.id %>" tabindex="-1" role="dialog" aria-labelledby="exampleModalLongTitle" aria-hidden="true">
            <div class="modal-dialog" role="document">
              <div class="modal-content">
                <div class="modal-header">
                  <div id="header-modal-formation">
                    <img class= "rounded-logo" src= <%= formation.logo %>>
                    <h3 class="modal-title" id="exampleModalLongTitle" class="text-center"><%= formation.title%></h3>
                  </div>
                </div>
                <div class="modal-body">
                  <p><%= formation.description %></p>
                  <p><strong> Lieux : </strong><%= formation.location%></p>
                  <p><strong> Durée : </strong><%= formation.duration%></p>
                  <p> Pour en savoir plus sur cette formation : <a href=" <%=formation.website_url%>" class="tooltip-test" title="LinkSchool" target="_blank"> Le lien </a></p>


                  <div class="d-flex justify-content-center" data-aos="zoom-in">
                    <% if formation.bookmarked %>
                      <%= link_to job_bookmark_formation_path(@job, formation_id: formation.id), method: :post, remote: true do %>
                        <i class="fas fa-star star-user-formation" id="star-user-formation-<%= formation.id %>"><strong> Enlever des favoris</strong></i>
                      <% end %>
                    <% else %>
                      <%= link_to job_bookmark_formation_path(@job, formation_id: formation.id), method: :post, remote: true do %>
                        <i class="far fa-star star-user-formation" id="star-user-formation-<%= formation.id %>"><strong> Ajouter aux favoris</strong></i>
                      <% end %>
                    <% end %>
                  </div>


                </div>
              </div>
            </div>
          </div>
        <%end%>
      </div>
    </div>
  </div>

  <div id="Mentors" class="d-none tab-content">
    <div class="container mt-5">
      <h2>Rencontrez des mentors</h2>
      <p>Disponible très prochainement, nos meilleurs développeurs travaillent dessus...</p>
    </div>
  </div>
</div>

<div class="d-flex justify-content-center">
  <%= link_to "Revenir à l'index", jobs_path, class: "btn" %>
</div>















