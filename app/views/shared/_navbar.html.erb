<div class="navbar navbar-expand-sm navbar-light navbar-equal-path">
    <%= link_to image_tag("https://res.cloudinary.com/dovu27lye/image/upload/v1567082401/logo_ou0lpl.png"), @path_one, class:"logo_first" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto d-flex align-items-center">
      <li class="nav-item active">
      <%= link_to "Tableau de bord", users_path, class:"link mr-3" %>
      </li>
      <% if user_signed_in? %>
        <li class="nav-item dropdown">
          <div class="dropdown-toggle m-2" id="navbarDropdown" data-toggle="dropdown" aria-haspopup=true aria-expanded=true> <%= current_user.first_name %>
          </div>
          <div class="nav-item dropdown">
            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
              <%= link_to "Se déconnecter", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
            </div>
          </div>
        </li>
          <% if current_user.profile_picture != nil  %>
          <li>
            <%= cl_image_tag current_user.profile_picture, class: "avatar-square m-1", alt: "" %>
          </li>
          <% end %>
      <% else %>
        <div class="d-flex align-items-center">
          <li class="nav-item">
            <%= link_to "Se connecter", new_user_session_path, class: "link nav-link ml-3" %>
          </li>
        </div>
      <% end %>
    </ul>
  </div>
</div>

