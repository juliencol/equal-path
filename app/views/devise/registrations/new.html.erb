<div class="main-container">
  <h2 class="mb-3 text-center">Créer votre compte Equal Path</h2>
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :first_name,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "first name" },
                  placeholder: "Jean" %>
      <%= f.input :last_name,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "last name" },
                  placeholder: "Michel" %>
      <%= f.input :age,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "age" },
                  placeholder: "22" %>
      <%= f.input :email,
                  required: true,
                  autofocus: true,
                  input_html: { autocomplete: "email" },
                  placeholder: "jeanmichel@gmail.com" %>
      <%= f.input :password,
                  required: true,
                  hint: ("#{@minimum_password_length} characters minimum" if @minimum_password_length),
                  input_html: { autocomplete: "new-password" } %>
      <%= f.input :password_confirmation,
                  required: true,
                  input_html: { autocomplete: "new-password" } %>
      <div class="form-actions text-center">
        <%= f.button :submit, "Commencez l'aventure", class:"btn btn-primary mt-3 mb-5 text-center" %>
      </div>
      <p class="mt-5">Already have an account? <%= render "devise/shared/links" %></p>
    </div>
  <% end %>
</div>

